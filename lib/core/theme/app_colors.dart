import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();
  static const primary = Color(0xFF0056B3);
  static const primaryContainer = Color(0xFFEAF2FC);
  static const secondary = Color(0xFFB5470D);
  static const secondaryContainer = Color(0xFFFFECDD);
  static const brandOrange = Color(0xFFEC5B13);
  static const background = Color(0xFFF6F7F9);
  static const studentBackground = Color(0xFFFAF8F3);
  static const studentMint = Color(0xFFE3F5F1);
  static const studentTeal = Color(0xFF006B68);
  static const studentLilac = Color(0xFFF0ECFC);
  static const studentPurple = Color(0xFF66509B);
  static const studentYellow = Color(0xFFFFE8A3);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFEEF1F5);
  static const textPrimary = Color(0xFF202D3A);
  static const textSecondary = Color(0xFF526170);
  static const textDisabled = Color(0xFF677382);
  static const border = Color(0xFF8291A3);
  static const divider = Color(0xFFDCE2EA);
  static const success = Color(0xFF246448);
  static const successContainer = Color(0xFFEAF5EE);
  static const warning = Color(0xFF845008);
  static const warningContainer = Color(0xFFFFF3DB);
  static const error = Color(0xFFB42332);
  static const errorContainer = Color(0xFFFFECEE);
  static const info = primary;

  // Existing names remain compatible and resolve to the semantic palette.
  static const primaryBlue = primary;
  static const secondaryOrange = secondary;
  static const neutralDark = textPrimary;
  static const neutralGray = textSecondary;
  static const backgroundLight = background;
  static const surfaceWhite = surface;
  static const borderGray = border;
  static const mutedText = textSecondary;
  static const errorRed = error;
  static const privacyBackground = secondaryContainer;
  static const infoBlueLight = primaryContainer;
  static const teacherBackground = background;
  static const cardBorder = divider;
  static const warningBackground = warningContainer;
  static const warningRed = error;
  static const successGreen = success;
}
