import 'package:flutter/material.dart';
import 'app_logo.dart';

/// The existing TamizIA mascot accompanies preparation, outside measured tasks.
class StudentInstructionCard extends StatelessWidget {
  const StudentInstructionCard({super.key});
  @override
  Widget build(BuildContext context) => const AppLogo(size: 120);
}
